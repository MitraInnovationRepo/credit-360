import PropTypes from 'prop-types'
import React, { Component } from 'react'
import { Icon, Container, Input, Header } from 'semantic-ui-react'

class Dashboard extends Component {
  render() {
    return (
      <Container>
        <Header as='h3'>
          Main Page
          </Header>
      </Container>
    );
  }
}

export default Dashboard;
